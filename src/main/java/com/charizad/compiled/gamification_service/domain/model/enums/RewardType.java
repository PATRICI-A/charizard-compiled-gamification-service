package com.charizad.compiled.gamification_service.domain.model.enums;

/**
 * Categories of rewards that can be unlocked when a user surpasses an XP threshold.
 * No actual reward items exist yet — this enum is a placeholder for future content.
 */
public enum RewardType {
    /** A title the user can display next to their name. */
    TITLE,
    /** A decorative frame for the user's profile picture. */
    FRAME,
    /** A purely cosmetic item (theme, colour, etc.). */
    COSMETIC,
    /** Unlocks a platform feature or capability. */
    FEATURE
}

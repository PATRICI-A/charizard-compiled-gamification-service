server:
  port: ${PORT:8080}

spring:
  application:
    name: gamification-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://shaddaygonzalez5_db_user:UhQV85Jf9l2dYkpS@cluster0.grwrgmg.mongodb.net/gamification_db?appName=Cluster0}
      auto-index-creation: true

  security:
    jwt:
      secret: ${JWT_SECRET:charizard-compiled-secret-key-2025-very-long-key-for-hs256}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

services:
  notification:
    url: ${NOTIFICATION_SERVICE_URL:app-patricia-notification-b4d2hcd0fqd7bkcm.canadacentral-01.azurewebsites.net}

scheduling:
  weekly-reset:
    cron: "0 0 0 * * MON"   # Lunes a medianoche

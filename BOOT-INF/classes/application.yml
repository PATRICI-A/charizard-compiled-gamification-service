server:
  port: ${PORT:8080}

spring:
  application:
    name: gamification-service
  data:
    mongodb:
      uri: "${SPRING_DATA_MONGODB_URI:mongodb+srv://shaddaygonzalez5_db_user:UhQV85Jf9l2dYkpS@cluster0.grwrgmg.mongodb.net/gamification_db?appName=Cluster0}"

  security:
    jwt:
      secret: ${JWT_SECRET:P4tric1A-Auth-S3rv1c3-S3cr3t-K3y-2026!!}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

scheduling:
  weekly-reset:
    cron: "0 0 0 * * MON"   # Lunes a medianoche